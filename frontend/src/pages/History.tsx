import {useEffect,useMemo,useState} from 'react';import {Search,History as HistoryIcon} from 'lucide-react';import {api} from '../services/api';import RiskBadge from '../components/RiskBadge';
export default function History(){const [rows,setRows]=useState<any[]>([]);const [q,setQ]=useState('');useEffect(()=>{api.predictions().then(setRows)},[]);const filtered=useMemo(()=>rows.filter(r=>r.device_name.toLowerCase().includes(q.toLowerCase())),[rows,q]);return <div><div className="page-head"><div><div className="eyebrow">AUDIT TRAIL</div><h1>Risk history</h1><p>Every manual assessment is recorded here for review.</p></div></div><div className="toolbar"><div className="search"><Search size={17}/><input placeholder="Search device history…" value={q} onChange={e=>setQ(e.target.value)}/></div></div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>Device</th><th>Date</th><th>Score</th><th>Risk</th><th>Factors</th></tr></thead><tbody>{filtered.length?filtered.map(r=><tr key={r.id}><td><b>{r.device_name}</b></td><td>{new Date(r.prediction_time).toLocaleString()}</td><td><strong>{r.risk_score}</strong></td><td><RiskBadge risk={r.risk_level}/></td><td>{r.contributing_factors.slice(0,3).map((f:any)=>f.feature).join(' · ')}</td></tr>):<tr><td colSpan={5}><div className="empty-table"><HistoryIcon/><b>No prediction history yet</b><span>Run an assessment from a device profile.</span></div></td></tr>}</tbody></table></div></section></div>}
