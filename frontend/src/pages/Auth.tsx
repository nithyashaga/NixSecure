import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {ArrowLeft,ArrowRight,Eye,EyeOff,UserPlus,LogIn,ShieldCheck} from 'lucide-react';import Logo from '../components/Logo';import SecurityCore from '../three/SecurityCore';import {api} from '../services/api';
export default function Auth({mode}:{mode:'login'|'register'}){const nav=useNavigate();const [isRegister,setIsRegister]=useState(mode==='register');const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');if(isRegister&&password!==confirm){setError('Passwords do not match');return}setLoading(true);try{const data=isRegister?await api.register(name,email,password):await api.login(email,password);localStorage.setItem('nix_token',data.token);localStorage.setItem('nix_user',JSON.stringify(data.user));nav('/app')}catch(err){setError(err instanceof Error?err.message:'Unable to continue')}finally{setLoading(false)}}async function guest(){const data=await api.guest();localStorage.setItem('nix_token',data.token);localStorage.setItem('nix_user',JSON.stringify(data.user));nav('/app')}return <div className="auth-page"><div className="auth-visual"><div className="auth-copy"><Logo/><div className="pill">SECURITY INTELLIGENCE PLATFORM</div><h1>Risk visibility without the noise.</h1><p>Turn security indicators into clear, explainable decisions with NixSecure.</p></div><SecurityCore/></div><div className="auth-panel"><button className="back-btn" onClick={()=>nav('/')}><ArrowLeft size={17}/> Back</button><div className="auth-box"><div className="auth-icon">{isRegister?<UserPlus size={20}/>:<LogIn size={20}/>}</div><div className="eyebrow">NIXSECURE ACCESS</div><h2>{isRegister?'Create your account':'Welcome back'}</h2><p className="muted">{isRegister?'Set up your private project workspace.':'Continue to your security console.'}</p><form onSubmit={submit}>{isRegister&&<label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Nithya Shaga" required/></label>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label>Password<div className="password-wrap"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" required minLength={6}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{isRegister&&<label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="••••••••" required/></label>}{error&&<div className="error-box">{error}</div>}<button className="primary-btn full" disabled={loading}>{loading?'Securing session…':isRegister?'Create account':'Sign in'} <ArrowRight size={17}/></button></form><div className="divider"><span>or</span></div><button className="guest-btn" onClick={guest}><ShieldCheck size={17}/> Continue as Guest</button><div className="switch-auth">{isRegister?'Already have an account?':'New to NixSecure?'} <button onClick={()=>{setIsRegister(!isRegister);setError('')}}>{isRegister?'Sign in':'Create account'}</button></div><div className="demo-hint">Demo account: <b>demo@nixsecure.local</b> / <b>NixSecure123</b></div></div></div></div>}
