import {ShieldCheck,Info,LogOut} from 'lucide-react';import {useNavigate} from 'react-router-dom';
export default function Settings(){const nav=useNavigate();const user=JSON.parse(localStorage.getItem('nix_user')||'{"name":"Guest","email":"guest@nixsecure.local"}');return <div><div className="page-head"><div><div className="eyebrow">PREFERENCES</div><h1>Settings</h1><p>Account and prototype information.</p></div></div><div className="settings-grid"><section className="panel"><div className="settings-icon"><ShieldCheck/></div><div className="eyebrow">ACCOUNT</div><h3>{user.name}</h3><p className="muted">{user.email}</p><div className="setting-row"><span>Session type</span><b>{user.email.includes('guest')?'Guest':'Authenticated'}</b></div></section><section className="panel"><div className="settings-icon"><Info/></div><div className="eyebrow">ABOUT NIXSECURE</div><h3>Predict. Prioritize. Protect.</h3><p className="muted">NixSecure is an educational cybersecurity risk prediction prototype using synthetic/demo security data. It is not a replacement for production SIEM, EDR, vulnerability management, or enterprise security monitoring.</p><div className="setting-row"><span>Risk thresholds</span><b>0–30 Low · 31–60 Medium · 61–100 High</b></div></section></div><button className="danger-btn" onClick={()=>{localStorage.clear();nav('/')}}><LogOut size={16}/> Sign out</button></div>}
