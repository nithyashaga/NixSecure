import {useEffect,useState} from 'react';import {ArrowUpRight,ShieldAlert,ShieldCheck,Activity,MonitorSmartphone} from 'lucide-react';import {ResponsiveContainer,AreaChart,Area,XAxis,YAxis,Tooltip,PieChart,Pie,Cell} from 'recharts';import {api} from '../services/api';import RiskBadge from '../components/RiskBadge';import SecurityCore from '../three/SecurityCore';
export default function Dashboard(){const [data,setData]=useState<any>(null);useEffect(()=>{api.dashboard().then(setData).catch(()=>{})},[]);if(!data)return <div className="loading-screen">Loading security console…</div>;const pie=[{name:'High',value:data.counts.HIGH},{name:'Medium',value:data.counts.MEDIUM},{name:'Low',value:data.counts.LOW}];const COLORS=['#ff4d6d','#ffb84a','#35e0a1'];const trend=(data.recent||[]).map((x:any,i:number)=>({name:x.device?.split('-')[0]||`D${i+1}`,score:x.score||0}));return <div><div className="page-head"><div><div className="eyebrow">OVERVIEW</div><h1>Security overview</h1><p>Prioritize enterprise devices using AI-assisted risk intelligence.</p></div><div className="live-chip"><span className="status-dot"/> DATA CONSOLE ONLINE</div></div><div className="stat-grid"><Stat icon={<MonitorSmartphone/>} label="Total devices" value={data.total_devices}/><Stat icon={<ShieldAlert/>} label="High risk" value={data.counts.HIGH}/><Stat icon={<Activity/>} label="Medium risk" value={data.counts.MEDIUM}/><Stat icon={<ShieldCheck/>} label="Low risk" value={data.counts.LOW}/></div><div className="dashboard-grid"><section className="panel risk-overview"><div className="panel-head"><div><span className="eyebrow">PORTFOLIO RISK</span><h3>Current posture</h3></div><span className="score-mini">{data.overall_score || 0}<small>/100</small></span></div><div className="posture"><div className="donut"><PieChart width={180} height={180}><Pie data={pie} dataKey="value" innerRadius={58} outerRadius={78} stroke="none">{pie.map((_,i)=><Cell key={i} fill={COLORS[i]}/>)}</Pie></PieChart><div className="donut-label"><strong>{Math.round(data.overall_score||0)}</strong><span>overall</span></div></div><div className="legend">{pie.map((x,i)=><div key={x.name}><span className="legend-dot" style={{background:COLORS[i]}}/><span>{x.name}</span><b>{x.value}</b></div>)}</div></div></section><section className="panel chart-panel"><div className="panel-head"><div><span className="eyebrow">RISK TREND</span><h3>Recent assessment scores</h3></div><ArrowUpRight size={18}/></div><ResponsiveContainer width="100%" height={210}><AreaChart data={trend}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#29b9ff" stopOpacity={.4}/><stop offset="100%" stopColor="#29b9ff" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="name" hide/><YAxis domain={[0,100]} tick={{fill:'#6e7c96',fontSize:11}} axisLine={false} tickLine={false}/><Tooltip contentStyle={{background:'#0b1221',border:'1px solid #1e2a40',borderRadius:12}}/><Area type="monotone" dataKey="score" stroke="#29b9ff" strokeWidth={2} fill="url(#g)"/></AreaChart></ResponsiveContainer></section></div><div className="dashboard-grid second"><section className="panel table-panel"><div className="panel-head"><div><span className="eyebrow">RECENT ASSESSMENTS</span><h3>Latest device activity</h3></div></div><div className="table-wrap"><table><thead><tr><th>Device</th><th>Risk</th><th>Score</th><th>Last prediction</th></tr></thead><tbody>{data.recent.map((r:any)=><tr key={r.device}><td><b>{r.device}</b></td><td><RiskBadge risk={r.risk}/></td><td>{r.score??'—'}</td><td>{r.time?new Date(r.time).toLocaleString():<span className="muted">Not assessed</span>}</td></tr>)}</tbody></table></div></section><section className="panel mini-3d"><div className="panel-head"><div><span className="eyebrow">SECURITY CORE</span><h3>Device intelligence map</h3></div></div><div className="mini-scene"><SecurityCore compact/><div className="scene-caption"><span className="status-dot"/> Synthetic enterprise environment</div></div></section></div></div>}
function Stat({icon,label,value}:{icon:React.ReactNode;label:string;value:number}){return <div className="stat-card"><div className="stat-icon">{icon}</div><div><span>{label}</span><strong>{value}</strong></div></div>}
