import {Navigate,Route,Routes} from 'react-router-dom';import Landing from './pages/Landing';import Auth from './pages/Auth';import AppLayout from './layouts/AppLayout';import Dashboard from './pages/Dashboard';import Devices from './pages/Devices';import DeviceDetails from './pages/DeviceDetails';import Analysis from './pages/Analysis';import History from './pages/History';import Model from './pages/Model';import Settings from './pages/Settings';
function Protected({children}:{children:React.ReactNode}){return localStorage.getItem('nix_token')?<>{children}</>:<Navigate to="/login" replace/>}
export default function App(){return <Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Auth mode="login"/>}/><Route path="/register" element={<Auth mode="register"/>}/><Route path="/app" element={<Protected><AppLayout><Dashboard/></AppLayout></Protected>}/><Route path="/app/devices" element={<Protected><AppLayout><Devices/></AppLayout></Protected>}/><Route path="/app/devices/:id" element={<Protected><AppLayout><DeviceDetails/></AppLayout></Protected>}/><Route path="/app/analysis" element={<Protected><AppLayout><Analysis/></AppLayout></Protected>}/><Route path="/app/history" element={<Protected><AppLayout><History/></AppLayout></Protected>}/><Route path="/app/model" element={<Protected><AppLayout><Model/></AppLayout></Protected>}/><Route path="/app/settings" element={<Protected><AppLayout><Settings/></AppLayout></Protected>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
