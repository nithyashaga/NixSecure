import {NavLink,useNavigate} from 'react-router-dom';
import {LayoutDashboard,MonitorSmartphone,BrainCircuit,History,ChartNoAxesCombined,Settings,LogOut,ChevronRight} from 'lucide-react';
import Logo from '../components/Logo';
export default function AppLayout({children}:{children:React.ReactNode}){const nav=useNavigate();const user=JSON.parse(localStorage.getItem('nix_user')||'{"name":"Guest"}'); const items=[['/app','Overview',LayoutDashboard],['/app/devices','Devices',MonitorSmartphone],['/app/analysis','AI Risk Analysis',BrainCircuit],['/app/history','Risk History',History],['/app/model','Model Performance',ChartNoAxesCombined]] as const; return <div className="app-shell"><aside className="sidebar"><Logo small/><nav>{items.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/app'} className={({isActive})=>isActive?'nav-item active':'nav-item'}><Icon size={18}/><span>{label}</span><ChevronRight size={14} className="nav-arrow"/></NavLink>)}</nav><div className="sidebar-bottom"><NavLink to="/app/settings" className="nav-item"><Settings size={18}/><span>Settings</span></NavLink><button className="nav-item logout" onClick={()=>{localStorage.clear();nav('/')}}><LogOut size={18}/><span>Logout</span></button></div></aside><main className="main"><header className="topbar"><div><div className="eyebrow">NIXSECURE - Predict. Prioritize. Protect.</div><div className="top-title">Enterprise risk intelligence</div></div><div className="user-pill"><span className="status-dot"/> <span>{user.name||'Guest'}</span></div></header><div className="content">{children}</div></main></div>}
